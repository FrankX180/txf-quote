# 發佈鐵律（前端改完必做）

> 2026-09-01 訂。看盤頁是公開站，**本機改完 ≠ 老公看到**。

## 硬規則

改 `index.html`／`worker/yahoo-quote.js` 且要讓線上生效時，**同一輪必須做完這三件事**，禁止只改本機就回報「修好了」：

1. **升版本**  
   - 頁腳 `.ver`  
   - `purgeOldMinCache` 的 `VER`（兩處必須同號）  
   - 小改動：`2.11` → `2.12`（第三碼 +1）  
   - 升 VER 會清 `txfMin`／`txf1m`，這是故意的。

2. **commit + push `origin/master`**  
   - GitHub Pages：`https://frankx180.github.io/txf-quote/`  
   - 遠端常有 Actions 行情快照：不要跟 `data/*.json` 一起 rebase 打架；前端／Worker 用 cherry-pick 接到最新 `origin/master` 再推。  
   - **沒推上去 = 線上還是舊版。**

3. **改了 Worker 還要 deploy**  
   ```
   & R:\PythonProgram\Python312\python.exe E:\_Project\FuturesHTML\scripts\deploy_worker.py
   ```
   只 push JS 不會更新 `wtx.19850926.xyz`。

## 回報格式

先講線上版號，再講本機：

- 「已推 `master`，頁腳應為 **2.xx**；硬刷新，右下角不是這號就還沒吃到 Pages。」
- 不要在 Pages 還沒更新時，叫老公對 2.11 驗 2.12 的修。

## 禁止

- 改完只存本機、不 bump VER、不 push。  
- 用「Ctrl+F5 看看」當發佈。線上 CDN／Pages 吃的是 GitHub，不是 `E:\_Project\FuturesHTML`。  
- 把未推的修當作用戶已看到的修。
