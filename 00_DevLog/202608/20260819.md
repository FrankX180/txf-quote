# 2026-08-19 工作日誌

## 夜盤開盤走勢錯位／假高點 45206／均線歪

### 問題現象
夜盤一開盤：前面走勢錯位、黃色 36MA 歪掉；內外盤有新盤資料，價線卻像吃到另外一盤；圖上出現錯誤高點 **45206**；標籤「昨／開／高／低」也不對。

### 根因分析
- `bumpLiveBar`／`fillMinutes` 的 `sameSess` 只比 `sessKindAt`（都是 `night`），**跨曆日夜盤（昨晚→今晚）仍算同一盤** → 新棒沿用昨夜 `date`／開盤價。
- 當日 `lastDates(1)` 吃到上一夜 date → 整段昨夜畫進當日圖（含昨夜高 45206）。
- `sessOHLC`／`patchQuoteOHLC` 用「最後一根棒的 date」→ 開高低被昨夜污染。
- 內外盤用當夜 day key，所以副圖正常、主圖價線異常。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html`（v1.60） | `sameSess` 加比 `barDateAt`；本盤進行中 `lastDates`／`sessOHLC`／`patchQuoteOHLC` 強制本盤開盤日；`linePoints` 只留本盤開盤後的點 |

### 技術細節
- 跨夜重現：`2026-08-19 04:50` → `15:11`，舊 `sameSess=true`、新 `=false`；同夜連續分鐘仍 `true`。
- OHLC 模擬過濾後高點由 45206 改為本盤列（44699）。

### 驗證
- `99_TempScripts/-test_night_cut.js`：ALL PASS
- `99_TempScripts/-check_syntax.py`：OK
- Commit／push：`c04c1f3` → `origin/master`

### 一句話
夜盤開盤不再把上一夜整段畫進來；版本 1.60 已上線。

---

## 今日部位多／空又變空（奇摩空殼蓋過 OpenAPI）

### 問題現象
`[今日]` 頁多／空／淨顯示 `—`；線上 `uncovered.json` 日期已是 0819，但 `long`／`short` 全 `null`。

### 根因
昨天改 OpenAPI 主路後，舊 fallback 只在「奇摩完全沒列」才改用官方。今日 Actions 在奇摩暫時只有淨額、多空仍空時寫入；因奇摩日期較新（0819＞OpenAPI 0817），空殼仍勝出。

### 修正
1. `scripts/fetch_uncovered.py`：兩邊都有多空才比日期；奇摩無多空且 OpenAPI 有 → 用 OpenAPI。
2. 強制重抓：0819 多空已回填（外資 6692／88663 等）。

### 驗證
- 本機 `inst` 多空非 null；選源單測：hollow-yahoo → prefer OA PASS
- Commit／push：`57bc815`（選源＋`uncovered.json`）、`e1074fc`（本日誌）→ `origin/master`
- Pages `data/uncovered.json`：`LS_ok=True`（外資 6692／88663.2）


---

## Pre-compact savelog (2026-08-19 19:55)

### 問題現象
Session 在 refined-MD compact／reseed 前自動落地工作日誌。

### 摘要
- Session: `01a019a6-eeff-7573-8903-905e6532c1e5`
- Source: `C:\Users\AnyUser\.grok\sessions\E%3A%5C_Project%5CFuturesHTML\01a019a6-eeff-7573-8903-905e6532c1e5\chat_history.jsonl`
- Turns: user=11, assistant=10
- Last user: 外資投信 別做散戶的顏色 可以 但是 我要請你 上面的表頭文字 用灰色就好了 然後這些柱狀體，你不要把它做成圓角，你用一般的就可以。 色彩的透明度調整為50%
- Refined MD: `C:\Users\AnyUser\.grok\sessions\E%3A%5C_Project%5CFuturesHTML\01a019a6-eeff-7573-8903-905e6532c1e5\01a019a6-eeff-7573-8903-905e6532c1e5.md`
- Compact trigger: auto

### 驗證
pre_compact builtin savelog wrote refined MD + this section.

---

## OI 表頭灰字／柱去圓角／透明度 50%（v1.68）

### 需求
- 外資／投信等表頭文字改灰色（不要跟柱同色）
- 橫柱不要圓角
- 分色柱透明度 50%

### 修正
- `index.html`：`oiColTh` 不再上色；`.oi-th-pick` 固定 `#6b7280`
- `.oi-sbar`／`.oi-inline-bar`／track `border-radius: 0`
- `oiColFill` alpha → `0.50`

### 驗證
- `-check_syntax.py`：OK
