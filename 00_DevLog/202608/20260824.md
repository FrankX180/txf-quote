# 2026-08-24 工作日誌

---

## 盤中內外盤不更新（+219 口看似凍結）

### 問題現象
1. 看盤頁內外盤長時間停在舊值（截圖約外 21027／內 20808、買超 +219 口）。
2. 預期開盤每 5 秒刷新並寫入雲端內外盤差。

### 根因分析
1. Cloudflare Cron 有跑，且 `price_1m`（px1m）當日約 **176** 根連續。
2. 同日 D1 `imb` 僅約 **13** 根，中間大空洞（例 08:46→10:31、11:08→11:37）。
3. `pollAndStore` 舊順序：**先**打奇摩 chart **再**打報價；第二槍常被擋 → chart／價棒寫成功、`appendImb` 失敗。
4. 無人看盤時主要靠 Cron；CDN HIT 也不跑 Worker 寫入 → 內外盤序列稀疏，畫面像卡死。
5. 畫面日盤口數另會扣夜盤收盤基準，淨值與「原始外−內」不同屬設計，但兩邊都應隨時間變動。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `worker/yahoo-quote.js` | `pollAndStore` 改先報價＋`appendImb`（`fetchYahooQuoteRetry` 最多 3 次），再 `backfillChart1m` |
| 2 | `index.html` | 盤中 `fetch(LIVE,{cache:"no-store"})`；每 30 秒 `?kind=poll` 強制寫 D1；停滯判定 sig 納入內外盤 |
| 3 | `scripts/poll_live_daemon.py` | 日誌改印 `imb.d`（舊碼一直 `d None`） |

### 驗證
1. CF API deploy Worker；bindings 仍含 `GH_TOKEN` + `IMB_DB`；cron `* * * * *` 仍在。
2. `?kind=poll` → `imb.ok=true`；修復後分鐘柱連續（11:45、11:46、11:47…）。
3. Pages 已含 `txfStorePollAt`／`no-store`。
4. 本機 daemon 背景跑：`ok day d -1735` 級每 5 秒寫入。

### Commit
- `21f96b6` 內外盤：Cron 改先寫報價再 chart，並強制盤中寫入
- `83e6c0d` poll daemon 日誌改印 imb.d

### 殘局／注意
1. 今日修復前空洞無法回補（奇摩無歷史內外盤序列）。
2. 無開頁、無 daemon 時，雲端保證仍是 Cron **約每分**一筆，不是 5 秒。
3. daemon 不開機自啟；重開機需再跑或開著看盤頁。
