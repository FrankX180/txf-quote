# 2026-08-20 工作日誌

## savelog：一分K輪詢重設縮放（v1.83）

### 問題現象
1. FORK 承接昨夜一分K定案（單價≠K棒；Yahoo chart＝OHLC；v1.82）。
2. 使用者放大一分K後，約每 5 秒資料輪詢會把視窗縮回預設，無法持續看局部。

### 根因分析
- `pollLive`（5s）→ `applyLive` → `render()` → `draw()` → `renderK()`。
- 每次 `renderK` 都 `applyNewData` + `fitKVisible`（`setBarSpace` + `scrollToRealTime`），等於強制重置縮放／平移。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html`（1.83） | `renderK(forceFit)`：輪詢走 tip `updateData` 或 `preserveKView(applyNewData)`；僅首次／切週期／`draw(true)` 才 `fitKVisible` |
| 2 | `99_TempScripts/-fix_k_zoom_preserve.py` | 精準替換腳本（避免 hashline 大段殘留） |
| 3 | `00_DevLog/202608/20260819.md` | 補 1.83 段落與驗證 |

### 技術細節
- `preserveKView`：套用前存 `getBarSpace`／`getOffsetRightDistance`／可視起點 timestamp；套用後還原。
- `kViewSig = session + "|" + kInt` 變更時視為需 fit。
- 使用者按 K 模式／週期 chip：`draw(true)`。

### 驗證
- `-check_syntax.py` OK（node unclosed=0）
- commit／push：`0dcf8ea`
- raw + Pages：頁尾 **1.83**，內文含 `preserveKView`

### 一句話
輪詢只更新K資料，不重設你的放大視窗；v1.83 已上線。
