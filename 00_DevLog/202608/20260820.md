# 2026-08-20 工作日誌

## savelog：一分K輪詢重設縮放（v1.83）

### 問題現象
1. FORK 承接昨夜一分K定案（單價≠K棒；Yahoo chart＝OHLC；v1.82）。
2. 使用者放大一分K後，約每 5 秒資料輪詢會把視窗縮回預設，無法持續看局部。

### 根因分析
- `pollLive`（5s）→ `applyLive` → `render()` → `draw()` → `renderK()`。
- 每次 `renderK` 都 `applyNewData` + `fitKVisible`（`setBarSpace` + `scrollToRealTime`），等於強制重置縮放／平移。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html`（1.83） | `renderK(forceFit)`：輪詢走 tip `updateData` 或 `preserveKView(applyNewData)`；僅首次／切週期／`draw(true)` 才 `fitKVisible` |
| 2 | `99_TempScripts/-fix_k_zoom_preserve.py` | 精準替換腳本（避免 hashline 大段殘留） |
| 3 | `00_DevLog/202608/20260819.md` | 補 1.83 段落與驗證 |

### 技術細節
- `preserveKView`：套用前存 `getBarSpace`／`getOffsetRightDistance`／可視起點 timestamp；套用後還原。
- `kViewSig = session + "|" + kInt` 變更時視為需 fit。
- 使用者按 K 模式／週期 chip：`draw(true)`。

### 驗證
- `-check_syntax.py` OK（node unclosed=0）
- commit／push：`0dcf8ea`
- raw + Pages：頁尾 **1.83**，內文含 `preserveKView`

### 一句話
輪詢只更新K資料，不重設你的放大視窗；v1.83 已上線。


---

## Pre-compact savelog (2026-08-20 09:27)

### 問題現象
Session 在 refined-MD compact／reseed 前自動落地工作日誌。

### 摘要
- Session: `01a01cb3-9a91-75f2-9e5e-b5368d3731ae`
- Source: `C:\Users\AnyUser\.grok\sessions\E%3A%5C_Project%5CFuturesHTML\01a01cb3-9a91-75f2-9e5e-b5368d3731ae\chat_history.jsonl`
- Turns: user=3, assistant=2
- Last user: 那麼我希望你在夜盤結束後，能夠取得數據的時候，就一起把這裡更新數據上去，顯示8/20的數據(夜)  比如 8/20(夜)  XXXXX  XXXXX  XXXXX  這樣雖然只有一半的數據，但是還是可以看到相關的資訊
- Refined MD: `C:\Users\AnyUser\.grok\sessions\E%3A%5C_Project%5CFuturesHTML\01a01cb3-9a91-75f2-9e5e-b5368d3731ae\01a01cb3-9a91-75f2-9e5e-b5368d3731ae.md`
- Compact trigger: auto

### 驗證
pre_compact builtin savelog wrote refined MD + this section.

---

## 法人未平倉加夜盤成交列（v1.84）

### 需求
夜盤結束且期交所能抓到後，法人未平倉表最上列顯示 `8/20(夜)` 等列（成交淨口，非 OI）。

### 改動
| # | 檔案 | 內容 |
|---|---|---|
| 1 | `scripts/when.py` | `want_uncovered` 加平日／週六 08:00–10:00 |
| 2 | `scripts/fetch_uncovered.py` | 爬 `futContractsDateAh` → `uncovered.json.night` |
| 3 | `index.html`（1.84） | 最上列 `(夜)`；註記為夜盤成交淨口 |
| 4 | `data/uncovered.json` | 寫入 20260820 night |

### 驗證
- `TXF_FORCE=1` fetch：`src ...+night_ah`，night=`20260820` foreign≈−57.15 dealer≈182.05
- `check_js.py` brace/paren 0

---

## 法人未平倉夜盤列上線與 push 搶權根治（接續）

### 問題現象
1. 本機 1.84 有 `8/20(夜)`，線上截圖仍 1.83、表從 8/19 起無夜盤列。
2. `git push`／舊 push helper 對 `FrankX180/txf-quote` 反覆 403（User `GITHUB_TOKEN` fine PAT 搶權）。

### 根因分析
- Pages 未更新＝本機超前未成功 push。
- User env `GITHUB_TOKEN`（`github_pat_…`）蓋掉 gh keyring `gho_…`，對此 repo 無寫入權。

### 修正內容

| # | 檔案／位置 | 修正內容 |
|---|---|---|
| 1 | `scripts/when.py`／`fetch_uncovered.py`／`index.html`／`data/uncovered.json` | v1.84：夜盤成交淨口 → `uncovered.night`；最上列 `M/D(夜)` |
| 2 | User env | `GITHUB_TOKEN` 改名 `GITHUB_FINE_PAT`；舊名 User UNSET |
| 3 | `E:\_PluginTools\GitHub\git_push_safe.py` | 剝奪搶權 env、FrankX180 keyring、fetch/rebase、extraheader push |
| 4 | SafetyGuards `encoding_safety.check_git_push_auth` + terminal/powershell guard | 裸 `git push` ADVISORY 引導正本 |
| 5 | `~/.claude/Claude.md` tools | GitHub push 🩸 正本入口 |
| 6 | `台指期看盤頁-說明書.md` | 推送指令改走 git_push_safe |

### 驗證
- fetch：`night=20260820` foreign≈−57.15 dealer≈182.05；raw `ver=1.84` + night JSON
- 清 token／keyring push：`9720e14`（1.84）、說明書 `52b9ea4` → `OK push`
- 偽造 process `GITHUB_TOKEN` dry-run 仍 `token_prefix gho_ len 40`
- Claude commit：`8d7f839`

### 一句話
夜盤列已上線；之後本機 push 一律走 `git_push_safe.py`，勿再設 User `GITHUB_TOKEN`。

---

## 法人未平倉夜盤列改估水位（v1.85）

### 問題現象
水位模式下，最上列 `08/20(夜)` 外資／投信／自營／散戶顯示約 −50／0／200／−100。老公指出應納入昨收日盤後的夜盤成交，水位仍須是八萬口級，不能變負五十。

### 根因分析
v1.84 夜盤列故意寫「當夜三大法人成交淨口（非未平倉水位）」，前端直接把 `uncovered.night.foreign` 等畫進水位表，與「法人未平倉」語意衝突。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `scripts/fetch_uncovered.py` | `night.foreign` 等＝日盤 OI＋夜盤成交；成交另存 `*Delta`；舊 night 無 Delta 時回填 |
| 2 | `index.html`（1.85） | 水位用估水位；淨進出用當夜成交；舊 JSON 無 Delta 時前端加回日盤 |
| 3 | `data/uncovered.json` | 重抓 night 估水位 |

### 驗證
- refetch：`night.foreign=-82028.35`＝`hist0.foreign(-81971.2)+foreignDelta(-57.15)`
- `brace_delta 0`／`paren_delta 0`；`ver=1.85`
- commit／push：`877186c`（GATE-2 backup `Backup/uncovered.json-20260820-121445`）

### 一句話
夜盤列水位＝日盤未平倉＋當夜成交淨口；純成交只留在淨進出／`*Delta`。