# 2026-08-26 工作日誌

## 走勢３日／４日切換圖不變

### 問題現象
1. 看盤頁按「３日」「４日」走勢，圖表看起來幾乎不變。

### 根因分析
1. Yahoo 週五夜偶發留下六／日假 `date`（例：`20260823`）。
2. `lastDates` 把假日期算進多日天數名額。
3. 畫圖時這些棒牆鐘落在休市（`sessKindAt === "closed"`），全部濾掉。
4. 結果：４日 keep 多一個假日，可視結果 ≈ ３日。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html` | `lastDates` 只收「至少有一根開盤中棒」的 date；優先排序後的 1 分 K；不足再掃 5／15；VER **2.08** |

### 驗證
1. 線上 `kline-minute.json`：OLD span4=`[26,25,24,23]`；NEW span4=`[26,25,24,21]`。
2. `check_js.py`：brace／paren 0。

### Commit
- `c5d6a7d`／後 rebase 為 `0226331` 走勢３／４日會變了：天數只算有開盤的日期（已 push）

---

## 特法&VIX 標籤縮短

### 問題現象
1. 特法&VIX 分頁表頭「特定法人」「前十淨」偏長。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html` | 表頭／副圖名：特定法人→特法、前十淨→十大；說明文字同步 |

### Commit
- `8f1ad5c` 特法&VIX 表頭：特定法人改特法、前十淨改十大

---

## 今日 VIX 空白＋改抓 MIS 即時

### 問題現象
1. 特法&VIX 今日 VIX 空；P/C%／收盤有值。
2. 截圖時官網波動率頁已有 28.67。

### 根因分析
1. 14:55 抓檔時月檔／本地尚無今日 VIX。
2. `day_chips_ready` 籌碼齊後（或 15:00）定案 → `want_uncovered` 不再重抓。
3. VIX 月檔常晚於籌碼；MIS `getQuoteListVIX` 盤後即有收盤。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `scripts/when.py` | 缺今日 VIX 且 <17:00 → 不定案 |
| 2 | `scripts/fetch_uncovered.py` | 新增 `fetch_mis_vix_last`（proxy）；覆蓋當日；Actions 加 WEBSHARE_PROXIES |
| 3 | `data/uncovered.json` | 強制重抓後 today.vix=28.67 |

### 驗證
1. MIS probe：`CLastPrice=28.67`／`CDate=20260826`。
2. `TXF_FORCE=1` 印 `OK mis vix {20260826: 28.67}`。

### Commit
- `eabb84a` VIX 缺值時 17:00 前不定案，並補上今日 28.67
- `126ba35` VIX 改抓 MIS 即時收盤，盤後不再等月檔

---

## 法人／特法表寬＋收盤欄排版

### 問題現象
1. 「收盤(點)」中間空白偏大；手機右側欄易被切且難滑。
2. 表頭曾縮成「收(點)」；收盤價需獨立格；對齊先左後改右。
3. 網頁仍顯示 2.06（頁腳寫死，與內部 VER 脫節）。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html` | 收盤價／漲跌拆兩格、間距緊；表可橫滑；≤650px 再縮；表頭「收盤(點)」；對齊改靠右；頁腳／VER **2.11** |

### Commit
- `909d542` 法人／特法表收緊收盤欄，窄螢可橫滑不切欄
- `c53cd78` 收盤價獨立靠左一格，表頭恢復收盤(點)；VER 2.09
- `8ef0202` 收盤(點)改靠右對齊；VER 2.11
- `0ee3727` 頁腳顯示版號改為 2.11

---

## 法人表開頁慢

### 問題現象
1. 開頁很久才出現法人未平倉數字。

### 根因分析
1. `loadKline` 串行：日Ｋ（~3MB）→ 分Ｋ（~1.6MB）→ live1m → 才抓 `uncovered.json` → `renderOi`。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `index.html` | `loadUncover` 與日Ｋ並行；`txfUncoverCache` 先畫；VER **2.10**→後續頁腳併為 **2.11** |

### 驗證
1. `check_js.py` brace／paren 0。
2. raw GitHub：`foot 2.11`、`VER 2.11`。

### Commit
- `ab525d1` 法人表開頁加速：uncovered 與日Ｋ脫鉤＋本機快取先畫


---

## GitHub update-quote startup_failure ＋ heal 節流

### 問題現象
1. GitHub 信：`FrankX180/txf-quote` `update-quote` Run failed at startup／No jobs were run。
2. 同時堆積大量 `workflow_dispatch`（約每 1–2 分一場）。

### 根因分析
1. 失敗 run（例 `32984737937`）annotation：GitHub Actions unexpected error（平台偶發）；同 SHA 另有成功場次 → 非 YAML／抓檔腳本壞掉。
2. Worker heal 過嚴：`HEAL_STALE_MS=35s`、`HEAL_MIN_INTERVAL_MS=60s`，狂打 dispatch + concurrency cancel-in-progress → 互搶、佇列異常。
3. 即時 5 秒報價／1 分 K 主路徑不依賴 heal 激進節流（Worker／Actions `*/5`）。

### 修正內容

| # | 檔案 | 修正內容 |
|---|---|---|
| 1 | `worker/yahoo-quote.js` | `HEAL_STALE_MS`／`HEAL_MIN_INTERVAL_MS` 皆改 **8 分** |

### 驗證
1. `deploy_worker.py`：upload True；`wtx.19850926.xyz` domain True；cron 仍 `* * * * *`。
2. 程式內常數已為 `8 * 60 * 1000`。

### Commit
- `c55bd71` heal 過期門檻與最短間隔改為 8 分鐘