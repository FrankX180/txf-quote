# 2026-09-07 工作日誌

## 任務摘要
- 修正台指期日 K 與即時合成鍵（期交所標準交易日 T）：將「前一交易日 15:00 夜盤 + 當日 08:45 日盤」正則化為完整日 K。
- 解決 9/7（一）日 K 缺口問題：週五 9/4 夜盤（開 46622 / 低 46560）與週一 9/7 日盤（高 47572 / 收 47470）完整合成 9/7 日 K。

## 改動明細
1. `index.html`（升版至 2.21）：
   - 新增 `barTradingDay(b)`：精確將分 K 棒歸屬至期交所交易日（夜盤歸下一交易日，週五夜歸週一）。
   - 新增 `currentTradingDayKey(ts)`：精確計算當前所屬交易日（15:00 起夜盤歸下一交易日）。
   - 更新 `synthDayBar` 與 `dayBarsLive`：使用 `barTradingDay(b)` 與 `currentTradingDayKey()` 進行交易日合併，徹底消除跨週夜盤斷層。
   - 更新 `klineData` 1 分 K 篩選：加入 `barTradingDay` 比對，避免單日檢索漏掉週五夜盤。
2. `data/kline-daily.json` & `data/kline-minute.json`：執行 `fetch_kline.py` 與 `fetch_minute.py` 補齊最新行情資料。

## 驗收確認
- 9/7 完整交易日日 K：O=46622.0, H=47572.0, L=46560.0, C=47470.0, V=76501。
- 頁面版本號同步升版至 2.21（頁腳與 `txfMinVer` 同號）。
