# 2026-09-26 開發日誌

## 1. 任務背景與問題定位
- **使用者回報**：晚間 22:08 收到 Cloudflare 寄出的「Workers CPU 限制已超出（100+ 次）」通知信，擔心今天稍早的調校仍未根治問題。
- **資料驗證與回溯**：
  - 透過 Cloudflare GraphQL Analytics API（`workersInvocationsAdaptive`）調閱過去 36 小時各整點維度之調用狀態、次數與 CPU P50/P99 數據。
  - 證實 2026-09-26 06:00 起至 23:00 止，連續 18 個小時 `exceededResources` 次數全數為 0，且 CPU P50 穩定維持在 0.9ms～1.7ms 極低水位。
  - 22:08 收到告警信之原因為 Cloudflare 採「滾動 24 小時（Rolling 24 Hours）」統計窗口，該時間點往前推 24 小時剛好涵蓋 9/25 22:00～9/26 05:00（上午未改版前夜盤）累計之 396 次歷史超標記錄，非改版後之新事件。

## 2. 當日各項優化與架構交付總結
- **定時任務瘦身（v2.31）**：
  - 改造 `barsFromChartJson`：日常增量模式下僅計算尾端 12 根 K 棒，避免 500 根歷史 K 棒的全量時區換算與字串格式化迴圈，CPU 耗時由 6ms 降至 0.1ms。
  - Scheduled 排程調度分流：每分鐘排程只執行輕量報價與五檔寫入（`{ chart: false }`），每 5 分鐘整點才執行一次尾端 chart 補漏，平日每分鐘 CPU 壓制在 1.5ms 內。
- **颱風假／休市自動熔斷器（Circuit Breaker）（v2.32）**：
  - 頂層 0ms 攔截（`shouldRunCron`）：週末（週六 05:10～週一 08:44）與日間休市空窗，不啟動任何非同步任務與 D1 查詢，定時器直接以 0ms CPU 結束。
  - 颱風假／國定假自適應熔斷：開盤 15 分鐘後自動檢查市場撮合成交狀態；若 Yahoo 標記 close 或成交時間戳停滯超過 1 小時，自動觸發當盤休眠至收盤，徹底避免全天無效輪詢。
  - 外部 API 異常退避保護（Backoff）：連續失敗 2 次後自動冷卻 5 分鐘，避免故障時死磕重試。
  - 時區邊界校準：`sessionEndMs` 收盤時間戳改採 `Date.UTC` 嚴格換算，消除 Worker 預設 UTC 時區下調用 `setHours` 引起的 8 小時偏差。
- **主網域統一升級（v2.33）**：
  - 將 Worker 綁定至主品牌網域 `wtx.blok.trading`，並維持 `wtx.19850926.xyz` 舊別名相容。
  - 前端 `index.html` 之 `LIVE` 報價端點切換為 `https://wtx.blok.trading/`。
  - `scripts/deploy_worker.py` 升級為支援雙網域自動維護。
  - 同步校準 `AGENTS.md`、`README.md`、`worker/AGENTS.md` 及說明書。

## 3. 部署與線上實測
- deployed worker `wtx.blok.trading`（同時維護 `wtx.19850926.xyz`）。
- 實測端點 `https://wtx.blok.trading/?kind=poll` 回傳 200 OK（`skipped: true, reason: 'closed'`）。
- 實測前端 `https://frankx180.github.io/txf-quote/` 頁腳已升至 2.33。
