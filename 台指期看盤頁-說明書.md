# 台指期近月看盤頁 — 完整說明書

| 項目 | 內容 |
|------|------|
| 專案路徑 | `E:\_Project\FuturesHTML` |
| GitHub | `https://github.com/FrankX180/txf-quote` |
| 靜態站（Pages） | `https://frankx180.github.io/txf-quote/` |
| 即時 Worker | `https://wtx.19850926.xyz/` |
| Cloudflare 帳號 | Worker 綁 `fx0926@gmail.com`（帳號 id 見 `scripts/deploy_worker.py`） |
| 說明書版本 | 2026-08-14（對應前端約 **v20260814-0089** 起） |
| 目的 | 給未來的自己／AI 接班：網站做什麼、資料從哪來、怎麼部署、怎麼下載內外盤差、流量與限制 |

---

## 1. 這個網站是什麼

單頁 **台指期近月（奇摩代號 `WTX&`）** 看盤工具，可公開分享：

- 最新價、漲跌、今開／高／低／昨收、總口數  
- 五檔買賣  
- 內外盤口數與「外盤 − 內盤」差值  
- 當日／多日 **走勢折線**（含副圖：內外盤差分鐘柱）  
- **K 線** 1／5／15／30 分、日 K、週 K  
- 法人未平倉、前十大交易人（收盤後資料）  

**不依賴本機常開瀏覽器才能給別人看報價**（報價走 Cloudflare Worker）。  
**內外盤差雲端序列** 則需要：有人開頁打 Worker、或 Cron、或本機守護，才會持續寫入。

---

## 2. 目錄結構

```
FuturesHTML/
├── index.html                 # 前端唯一主檔（HTML + CSS + JS）
├── README.md                  # 極短入口
├── 台指期看盤頁-說明書.md      # 本檔
├── data/                      # 靜態／Actions 寫入的 JSON（進 GitHub → Pages）
│   ├── snapshot.json          # 報價快照（含日／夜五檔結構）
│   ├── history-day.json       # 日盤 5 秒價量歷史（收盤歸檔用，非盤中主力）
│   ├── history-night.json
│   ├── kline-minute.json      # 1m/5m/15m 分日夜
│   ├── kline-daily.json       # 日／週／月 K、夜盤日 K 等
│   ├── uncovered.json         # 法人未平倉
│   ├── imb-YYYYMMDD.json      # 當日內外盤差分鐘序列冷備
│   └── imb-latest.json
├── worker/
│   └── yahoo-quote.js         # Cloudflare Worker 原始碼
├── scripts/
│   ├── when.py                # 台北時段閘門（Actions 用）
│   ├── fetch_quote.py         # 奇摩報價／五檔 → snapshot
│   ├── fetch_minute.py        # 奇摩分鐘線 + 收盤富邦補 1 分
│   ├── fetch_kline.py         # MoneyDJ 日／週／月 K
│   ├── fetch_uncovered.py     # 法人未平倉
│   ├── fetch_imb.py           # 從 Worker 拉 D1 內外盤差 → data/imb-*.json
│   ├── poll_live_daemon.py    # 本機每 5 秒 ?kind=poll 寫 D1
│   └── deploy_worker.py       # 部署 Worker + 綁 D1
├── relay/                     # 可選本機轉發（歷史／除錯用，Pages 主路徑不依賴）
├── .github/workflows/
│   └── update-quote.yml       # 每 5 分跑抓檔 + commit
├── 00_DevLog/                 # 開發日誌
│   └── 202608/20260814-取數架構.md
├── 99_TempScripts/            # 探測腳本（非生產）
└── Backup/                    # 資料檔備份（勿當 SSOT）
```

---

## 3. 總架構（三層）

```
┌─────────────────────────────────────────────────────────────┐
│  瀏覽器  frankx180.github.io/txf-quote/                      │
│  · 開盤且分頁可見：每 5 秒 GET Worker（報價）                  │
│  · 開頁／約 30 秒：GET Worker ?kind=imb（內外盤差）            │
│  · 慢變資料：GET GitHub Pages 的 data/*.json（吃 304）        │
│  · 本機：localStorage 暫存 1 分 K、內外盤差備援                 │
└───────────────┬───────────────────────────┬─────────────────┘
                │ 5 秒報價 / imb / poll      │ 靜態 JSON
                ▼                           ▼
┌───────────────────────────┐   ┌──────────────────────────────┐
│ Cloudflare Worker         │   │ GitHub Pages = repo 靜態檔     │
│ wtx.19850926.xyz          │   │ data/snapshot, kline-*,       │
│ · 代打奇摩 API            │   │   uncovered, imb-*.json       │
│ · CDN 快取 5 秒（報價）    │   └──────────────▲───────────────┘
│ · D1 庫 txf-imb 寫讀內外差│                  │ commit
│ · Cron 每 1 分自打寫 D1   │   ┌──────────────┴───────────────┐
└───────────┬───────────────┘   │ GitHub Actions 每 5 分         │
            │                   │ fetch_quote / minute / kline   │
            │ 奇摩              │ / uncovered / imb → push       │
            ▼                   └──────────────────────────────┘
     Yahoo 台股 API
     （WTX& 等）
```

### 3.1 為什麼這樣拆

| 資料性質 | 放哪 | 原因 |
|----------|------|------|
| 盤中最新價、五檔、即時內外盤 | **Worker** | 5 秒級；不 commit 爆 GitHub |
| 已走完分鐘 K、日 K、法人 | **GitHub `data/`** | 慢變、可 304、適合 Pages |
| 內外盤「分鐘序列」雲端 | **D1 `txf-imb`** | 每分鐘 UPSERT；免費額度夠；R2 未開通時用 D1 |
| 內外盤冷備 | **GitHub `data/imb-*.json`** | 可 diff、可下載、Pages 可直接開 |

**不要**把盤中 5 秒全量價寫回 GitHub。  
**不要**用 R2 當目前內外差主存（帳號若未在 Dashboard 開通 R2 會 10042；D1 更適合「每分鐘一列」）。

---

## 4. 前端（`index.html`）行為

### 4.1 畫面區塊

1. **報價頭**：最新價、漲跌％、時間戳、LIVE 狀態、迷你日 K  
2. **模式列**：K 線粒度／走勢「當日｜2～5 日」  
3. **走勢 canvas** 或 **K 線（klinecharts）**  
4. **當日** 走勢下方副圖：**內外盤差** 分鐘柱  
5. **五檔 + 內外盤長條 + 買賣超文字**  
6. **法人／前十大** 表  
7. 右下角版本字串（例 `v20260814-0089`）

### 4.2 盤別

- **日盤**：約 08:45–13:45（台北）  
- **夜盤**：約 14:55–次日 05:00（週六 05:10 後、週日休）  
- 前端 `liveSess()`／Worker `sessionOf()` 對齊；夜盤開盤對齊 **14:55**。

### 4.3 走勢折線用棒粒度（`minuteSeries`）

| 走勢選項 | 資料粒度 |
|----------|----------|
| 當日 | **1 分 K**（`day_1m` / `night_1m`） |
| 2 日 | **5 分 K** |
| 3～5 日 | **15 分 K** |

- 多日會 **合併日＋夜** 分鐘包（`packBars` 在 `daySpan >= 2` 時 join）。  
- 缺檔時：用更細棒 **重採** 成目標粒度；仍不足才退下一級。  
- **當日** 額外：最後一根之後併入 5 秒 `hist` 點（僅 `typeof ts === "number"` 的即時 tick），讓這一分鐘內線會動。  
- **多日 X 軸**：用「開盤視窗」串接（日 08:45–13:45、夜 15:00–次日 05:00）。**未走完的盤仍拉滿視窗長度** → 右側空白。  
- **多日不畫底部 X 軸日期文字**（避免重疊）；長按出 **十字線** 讀時間與價格。  
- 當日才畫小時刻度文字。  
- 最高／最低標記：**小實心圓**（非三角）。

### 4.4 內外盤差副圖（僅「當日」走勢）

- 每分鐘 1 根柱，**柱寬 1px**。  
- 值 `d = 外盤 − 內盤`（≥0 紅，&lt;0 綠）。  
- 來源合併：雲端 `?kind=imb` ＋ 本機 `localStorage` 鍵 **`txfImbMin`**。  
- 2～5 日走勢 **不畫** 此副圖。

### 4.5 五檔

- `fetch_quote.py` 日／夜 **分開寫**；非本盤沿用上一版乾淨五檔。  
- 與當盤同 bid/ask 視為污染並清除。  
- 前端 `lastBook`：非本盤不覆寫，避免早上看夜盤卻被日盤盤前檔污染。

### 4.6 輪詢與省流量

| 條件 | 行為 |
|------|------|
| 開盤 + 分頁可見 | 約 **每 5 秒** 打 Worker 報價 |
| 分頁隱藏 | **停** 報價輪詢 |
| 收盤 | **停** |
| 開盤後價＋量＋時間連 **10 次** 不變 | 改 **3 分鐘** 探測（颱風／無量） |
| GitHub JSON | **不要** 加 `?t=` 亂數；用 `cache: "no-cache"` 吃 **304** |
| 切日夜、2～5 日 | 盡量 **零網路**（本機已載入的 minute pack） |

### 4.7 本機 localStorage 鍵（重點）

| 鍵 | 用途 |
|----|------|
| `txfMin` | 近幾日 1m/5m/15m 暫存 |
| `txfImbMin` | 本機內外盤差分鐘柱備援 |
| `txfLastBook` | 最後乾淨五檔 |
| `txfStalePause` | 熔斷探測狀態 |

清快取／換裝置會丟本機備援；**雲端以 D1 + GitHub imb 為準**。

---

## 5. Worker（`worker/yahoo-quote.js`）

### 5.1 端點

| URL | 作用 |
|-----|------|
| `GET /` 或 `?kind=` 空 | 代打奇摩 **stockList**（WTX&、WCDF&、WCCF&），回 JSON body |
| `GET ?kind=1m` | 代打奇摩 **chart 1m**（前端主路徑已少用） |
| `GET ?kind=imb` | 讀 D1 當日（或 `day=`）內外盤差分鐘序列 |
| `GET ?kind=imb&day=20260814` | 指定日鍵 |
| `GET ?kind=poll` | **強制**打奇摩並寫 D1（**不走** 5 秒 CDN 語意；給本機守護） |

### 5.2 快取與計次

- 報價回應：`CDN-Cache-Control: public, max-age=5`  
- 須在 Cloudflare 打開 **Workers Cache**（`cache_options.enabled`）。  
- **CDN 命中不跑 Worker 程式 → 不計免費 10 萬次／日**。  
- 同一 PoP、多人每 5 秒刷新 → 約 **每 5 秒只 MISS 1 次** 去打奇摩。  
- 瀏覽器請求 **不要** 加防快取 query（會打穿快取）。  

`?kind=imb`：CDN max-age 約 **3 秒**。  
`?kind=poll`：`no-store`（每次真打）。

### 5.3 寫入 D1 的時機

1. **一般報價 MISS** 且非 `kind=1m`：`waitUntil(appendImb(...))`  
2. **Cron `scheduled`**：每分鐘 `pollAndStore`（CF Cron **最短 1 分**，無法免費 5 秒 Cron）  
3. **`?kind=poll`**：本機 `poll_live_daemon.py` 每 5 秒  

`appendImb`：僅開盤時段；`d = out − in`；主鍵 `(day_key, session, t)` 分鐘槽 **UPSERT**。

### 5.4 D1 綱要

庫名：**`txf-imb`**（uuid 見 devlog／`deploy_worker.py` 部署結果）

```sql
CREATE TABLE IF NOT EXISTS imb (
  day_key TEXT NOT NULL,   -- 交易日鍵 YYYYMMDD；凌晨夜盤歸前一曆日
  session TEXT NOT NULL,   -- 'day' | 'night'
  t INTEGER NOT NULL,      -- 分鐘槽 epoch ms
  d REAL NOT NULL,         -- 外盤 - 內盤
  inn REAL,
  outv REAL,
  ts INTEGER,              -- 最後寫入時間
  PRIMARY KEY (day_key, session, t)
);
```

### 5.5 部署

```powershell
& R:\PythonProgram\Python312\python.exe E:\_Project\FuturesHTML\scripts\deploy_worker.py
```

- 讀 Cloudflare API Key：`E:\_PluginTools\Memory\secrets\LLM_API_KEY.MD` 區塊（**勿把 key 寫進 repo**）  
- 上傳 `worker/yahoo-quote.js`、綁 D1、設定 routes／cron（以腳本現況為準）  
- Worker 名稱約 `txf-yahoo`；自訂網域 `wtx.19850926.xyz`

---

## 6. GitHub Actions（`update-quote.yml`）

- 觸發：`*/5 * * * *`、手動 `workflow_dispatch`、相關 scripts 變更 push  
- 步驟順序：  
  1. `fetch_quote.py`  
  2. `fetch_minute.py`（可失敗 continue）  
  3. `fetch_uncovered.py`  
  4. `fetch_kline.py`  
  5. `fetch_imb.py`  
  6. commit `data/*` 後 push  

### 6.1 時段閘門（`scripts/when.py`，台北）

| 函數 | 大致窗口 | 用途 |
|------|----------|------|
| `want_yahoo_quote` / `in_session` | 日 08:40–13:50、夜 14:55–05:10 | 奇摩報價／分鐘 |
| `want_fubon` | 13:50–14:40、05:00–08:30 | 富邦補完整 1 分 |
| `want_daily_k` | 收盤附近 | 日 K |
| `want_uncovered` / `want_tmf_retail` | 平日 14:30–20:00（另 08:00–10:00 補夜盤） | 法人／微台散戶 |
| 本機跑腳本 | 預設 **forced**（非 Actions 不擋） | 開發方便 |
| `TXF_FORCE=1` 或手動 workflow | 全跑 | 補資料 |

**富邦盤中不完整，禁止當即時源**；只在收盤窗口併入 `kline-minute.json`。

---

## 7. 資料流詳解

### 7.1 盤中報價（使用者開頁）

```
瀏覽器 5s → Worker（可能 CDN HIT）
              HIT  → 直接回快取 JSON（不計 Worker 次、不寫 D1）
              MISS → 打奇摩 → 回瀏覽器
                   → waitUntil 寫 D1 該分鐘內外差
```

### 7.2 無人看盤仍要寫內外差

| 層 | 間隔 | 說明 |
|----|------|------|
| Cloudflare Cron | **1 分** | 官方最短；保底分鐘級無缺口 |
| 本機 `poll_live_daemon.py` | **5 秒** | 電腦開著、開盤中；真·5 秒覆寫當分鐘 |
| 有人開頁 | **5 秒** | 報價 MISS 時一併寫 |

本機守護：

```powershell
& R:\PythonProgram\Python312\python.exe E:\_Project\FuturesHTML\scripts\poll_live_daemon.py
```

可設開機啟動／工作排程。休市腳本睡 30 秒。

### 7.3 分鐘 K

```
Actions 5 分 → fetch_minute
  盤中：奇摩 1m/5m/15m 併入 night_* / day_*
  收盤窗：富邦 CZMB 補齊全日／全夜 1m
       → data/kline-minute.json → Pages
瀏覽器：開頁載入 + localStorage；當日用 5 秒價 bump 當根 1m
```

日期欄位請統一 **`YYYYMMDD` 或可 `dateKey()` 正規化**（曾因 `20260814` vs `2026-08-14` 導致當日走勢空白）。

### 7.4 內外盤差雲端 → 冷備

```
D1 imb 表
  ↑ 寫：報價 MISS / Cron / poll
  ↓ 讀：?kind=imb
fetch_imb.py（Actions 或本機）
  → data/imb-YYYYMMDD.json + imb-latest.json
  → git commit → Pages 可下載
```

---

## 8. 內外盤差：格式、下載、限制

### 8.1 欄位語意

| 欄位 | 意義 |
|------|------|
| `dayKey` | 交易日鍵 `YYYYMMDD`（凌晨夜盤歸前一日） |
| `session` | `day` / `night`（在 D1 列內；JSON 則拆成兩個陣列） |
| `t` | 該分鐘起始 epoch **毫秒** |
| `d` | **外盤 − 內盤**（累計口數差在該分鐘最後一次觀測） |
| `in` / `out` | 內盤／外盤累計（寫入當下） |
| `ts` | 最後一次覆寫的時間 |

**粒度不是每 5 秒一根完整 tape**，而是：

> 每一分鐘最多一列；該分鐘內每次 5 秒觀測會 **覆寫** `d/in/out/ts`。

### 8.2 API 即時下載（整包當日）

瀏覽器或 curl：

```
https://wtx.19850926.xyz/?kind=imb
https://wtx.19850926.xyz/?kind=imb&day=20260814
```

回應形狀：

```json
{
  "dayKey": "20260814",
  "source": "d1",
  "day": [{ "t": 1786672680000, "d": -917, "in": 23095, "out": 22178, "ts": 1786672736220 }],
  "night": [],
  "updatedAt": "2026-08-14T02:27:42.422Z"
}
```

PowerShell 範例：

```powershell
Invoke-RestMethod "https://wtx.19850926.xyz/?kind=imb&day=20260814" |
  ConvertTo-Json -Depth 6 |
  Set-Content -Encoding utf8 E:\_Project\FuturesHTML\data\imb-manual.json
```

### 8.3 本機腳本歸檔

```powershell
cd E:\_Project\FuturesHTML
& R:\PythonProgram\Python312\python.exe scripts\fetch_imb.py
```

產出：

- `data/imb-YYYYMMDD.json`  
- `data/imb-latest.json`  

（檔內 `source` 字串可能仍寫舊註解 `worker r2`，實際來源是 Worker→**D1**。）

### 8.4 GitHub / Pages 冷備下載

| 方式 | URL／路徑 |
|------|-----------|
| 倉庫 raw | `https://raw.githubusercontent.com/FrankX180/txf-quote/master/data/imb-20260814.json` |
| Pages | `https://frankx180.github.io/txf-quote/data/imb-20260814.json` |
| 本機 clone | `E:\_Project\FuturesHTML\data\imb-*.json` |

**多日整包**：目前要 **按日** 打 API 或收集多個 `imb-YYYYMMDD.json` 再合併。  
尚未做：前端一鍵匯出、Worker `from/to` 多日、Release zip（可後續加）。

### 8.5 本機瀏覽器備援

開發者工具 → Application → Local Storage → `txfImbMin`：

```json
{
  "dayKey": "20260814",
  "bars": {
    "day": [{ "t": ..., "d": ..., "in": ..., "out": ..., "ts": ... }],
    "night": []
  }
}
```

僅該瀏覽器；清快取即沒。

### 8.6 內外盤差限制（必讀）

1. **不是交易所完整 tick tape**；只有「被寫入當下」的分鐘狀態。  
2. **缺口來源**：沒人開頁、Cron 尚未跑、本機守護關機、Worker/Yahoo 失敗、休市。  
3. Cron 只保證 **約每分鐘至少更新一次**；5 秒覆寫要本機 daemon 或有人看盤。  
4. 日盤／夜盤分陣列；畫圖要對齊 session 與 `t`。  
5. 內外盤奇摩有時為「千口」小數，Worker 會 `*1000` 正規化（見 `extractWtx`）。  
6. D1 免費額度（約略、以 CF 官網為準）：寫入約 **10 萬列／天**、讀約 **500 萬列／天**、庫約 **500 MB**。  
   - 本案用量：開盤若每 5 秒寫 ≈ 日盤 5h×720 + 夜盤 14h×720 ≈ **1.4 萬次寫／天** 量級（遠低於上限）。  
7. 歷史只會從「開始寫 D1 那天」累積；更早沒有魔法回補。

---

## 9. 流量與額度總表

### 9.1 Cloudflare Workers（免費約 10 萬 req／日）

| 行為 | 是否計 Worker 次 | 說明 |
|------|------------------|------|
| 報價 CDN **HIT** | **否**（理想） | 5 秒快取共享 |
| 報價 CDN **MISS** | **是** | 打奇摩 + 可能寫 D1 |
| `?kind=poll` | **是**（每次） | 本機 5 秒守護；開盤約 1.4 萬／天 |
| Cron 每分 | **是** | 開盤約日 300 + 夜 840 ≈ 千級／天 |
| `?kind=imb` | **是**（快取 3s 可省） | 前端約 30 秒一次／開頁 |

**設計目標**：百人同區同時看盤，報價 MISS 仍約 5 秒一次，而不是人數 × 5 秒。

滿額後行為依 CF 方案：免費常變慢／錯，**不會自動幫你刷卡**；監控 Dashboard。

### 9.2 D1

見 §8.6。讀寫都遠小於一般免費檔。勿做無意義的全表掃描迴圈。

### 9.3 GitHub Actions

每 5 分一次 job；時段外腳本多 SKIP，仍佔 runner 分鐘。免費額度對個人專案通常夠。

### 9.4 奇摩上游

Worker 與 Actions 都打奇摩；勿再加「每人直連奇摩」。被 ban 風險集中在 Worker IP／頻率；已用 5 秒聚合。

### 9.5 Git push 憑證注意

- **正本**：`& $py E:\_PluginTools\GitHub\git_push_safe.py --cwd E:\_Project\FuturesHTML`（或 `99_TempScripts\-push_v179.py`）。
- User env **勿再設 `GITHUB_TOKEN`**；細粒度 PAT 已改名 **`GITHUB_FINE_PAT`**（避免蓋掉 gh keyring 造成 403）。
- Actions 內建 `GITHUB_TOKEN`（contents: write）是雲端另一條路，與本機 PAT 不同。

---

## 10. 常用維運指令

```powershell
# Python
$py = "R:\PythonProgram\Python312\python.exe"
cd E:\_Project\FuturesHTML

# 手動抓全套資料（本機不過濾時段）
& $py scripts\fetch_quote.py
& $py scripts\fetch_minute.py
& $py scripts\fetch_kline.py
& $py scripts\fetch_uncovered.py
& $py scripts\fetch_imb.py

# 本機 5 秒寫 D1
& $py scripts\poll_live_daemon.py

# 部署 Worker
& $py scripts\deploy_worker.py

# 驗證 API
Invoke-RestMethod "https://wtx.19850926.xyz/?kind=imb"
Invoke-RestMethod "https://wtx.19850926.xyz/?kind=poll"

# 推 GitHub（安全入口；會剝奪搶權 token）
& $py E:\_PluginTools\GitHub\git_push_safe.py --cwd E:\_Project\FuturesHTML
```

前端改完：commit `index.html` → push → 等 Pages → 使用者 **Ctrl+F5** 看版本號。

---

## 11. 驗收清單（改完應自測）

- [ ] 開盤 LIVE 亮、約 5 秒價更新  
- [ ] 隱藏分頁後 Network 不再狂打 Worker  
- [ ] 當日走勢：1m 線 + 右側未走完時段空白（當日 sessRange）  
- [ ] 2 日：5m、含日夜、無底部 X 日期字、長按十字線有時間  
- [ ] 3～5 日：15m  
- [ ] 最高最低為 **小圓點**  
- [ ] 內外差副圖柱寬 **1px**；`?kind=imb` 有 `source:d1`  
- [ ] 收盤後 `kline-minute.json` 的 `day_1m` 根數 > 0（富邦窗口）  
- [ ] 夜盤早上看五檔不是日盤盤前污染  

---

## 12. 已知決策與未做

| 決策 | 狀態 |
|------|------|
| 盤中奇摩、收盤富邦 1 分 | 已定案 |
| 報價 5 秒 CDN 快取 | 已定案 |
| 內外差用 **D1** 非 R2 | 已上線；R2 可之後開但不急 |
| 內外差分鐘柱副圖 + 雲端 | 已上線 |
| 頁面一鍵下載多日 imb | **未做** |
| Worker 多日區間 API | **未做** |
| Windows 開機自動 poll daemon | **未做**（腳本已有，排程自訂） |
| Durable Object / Service Worker | **不做** |

---

## 13. 相關文件

| 檔案 | 內容 |
|------|------|
| `00_DevLog/202608/20260814-取數架構.md` | 取數原則短版 |
| `00_DevLog/202608/20260814.md` | 當日開發日誌 |
| `README.md` | 一行級入口 |
| 本檔 | **完整說明書（維運 SSOT 首選）** |

---

## 14. 接班 checklist（5 分鐘上手）

1. 開 Pages 看版本號與 LIVE。  
2. 開 `?kind=imb` 確認 D1 有資料。  
3. 看 `data/kline-minute.json` 最近 `day_1m`／`night_1m` 日期。  
4. 改前端只動 `index.html`；改取數動 `scripts/*`；改即時動 `worker/yahoo-quote.js` 後 **deploy_worker**。  
5. 推送前處理 `GITHUB_TOKEN` 403。  
6. 內外差下載：§8；流量：§9。  

— 完 —
